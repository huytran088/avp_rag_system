ANTHROPIC_API_KEY=your-key-here
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# CORS: comma-separated origins for deployed frontends
CORS_ORIGINS=https://huytran088.github.io

# LLM Provider: "anthropic" or "vllm"
LLM_PROVIDER=anthropic
# Optional fallback provider (leave empty to disable)
LLM_FALLBACK_PROVIDER=

# vLLM settings (only needed when LLM_PROVIDER=vllm or LLM_FALLBACK_PROVIDER=vllm)
VLLM_BASE_URL=http://vllm:8080/v1
VLLM_MODEL=Qwen/Qwen3-8B
VLLM_API_KEY=token-placeholder
