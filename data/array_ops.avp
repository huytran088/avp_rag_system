fun sum_array(numbers):
    total = 0
    
    // Iterate through the numbers array
    for n in numbers:
        total = total + n
    end for
    
    return total
end fun

fun create_array():
    // specific syntax for range arrays
    number_arr = [1 to 10]
    return number_arr
end fun